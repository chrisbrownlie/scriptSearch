#' Search for a string
#' 
#' A wrapper around gregexpr to make it easier to use in dataframes
#' 
#' @param string the string to search
#' @param text the text in which to search for string
#' 
#' @return a vector of integers denoting the starting points of each of the matches
find_string <- function(string, text) {
  results <- gregexpr(pattern = string,
                      text = text,
                      ignore.case = TRUE)
  if (length(results[[1]]) == 1) {
    return(results[[1]])
  } else {
    
  }
}